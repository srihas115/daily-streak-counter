SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: locks your deployment behind a secret URL token (?key=...).
# Leave unset for an open instance. Generate one with: openssl rand -hex 16
ACCESS_KEY=
